searches emoji respect to input value.

```jsx
<SearchBar
  input=""
  emojiContent={{ current: {} }} /* use ref here */
  onFieldSearch={() => {}}
  setInput={() => {}}
  setResults={() => {}}
  inputPlaceholder="🔎 Search emoji..."
/>
```
